# Backlog Notes

This file is a lightweight idea inbox only. Canonical task state lives in Beads
(`bd`), and approved behavior intent lives in OpenSpec.

## Candidate Ideas

- Fix overlay recovery after closing and reopening the laptop lid: the overlay can
  disappear from the primary monitor.
- Expand work-time selection with an interactive menu instead of only editing
  fixed schedule fields.
- Generate fresh overlay/rest texts periodically.
- Add Pomodoro-style analysis.

## Already Covered

- Overlay lock duration escalation is implemented and specified in
  `openspec/specs/overlay-lock-escalation/spec.md`.
