# Freeze current deferral period settings

During an active deferral period, WorkGuard saves settings changes as pending
period settings instead of applying them to current enforcement. Current period
settings persist across app restarts and remain active until the next work
period begins, so changing settings or restarting the app cannot reset overlay
deferral and escalation for the current period.
