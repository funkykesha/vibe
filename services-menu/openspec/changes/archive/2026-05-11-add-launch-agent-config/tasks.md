## 1. Запись LaunchAgent

- [x] 1.1 Добавить константы для директории LaunchAgents, корня логов и префикса label `com.agaibadulin`.
- [x] 1.2 Реализовать валидацию `name` как безопасного суффикса сервиса для label, имени файла и директории логов.
- [x] 1.3 Реализовать создание plist-payload с Label, ProgramArguments, опциональным WorkingDirectory, RunAtLoad, KeepAlive, StandardOutPath и StandardErrorPath.
- [x] 1.4 Реализовать файловую запись: создать директорию логов, отказать в перезаписи существующего plist и записать plist через `plistlib`.

## 2. Поток редактора в меню

- [x] 2.1 Добавить пункт меню для добавления конфига.
- [x] 2.2 Реализовать одно AppKit-окно редактора с полями `name`, `command`, `path-to-start` и опциональным `WorkingDirectory`.
- [x] 2.3 Добавить в окно кнопки "Отмена" и "Применить"; "Отмена" закрывает окно без записи.
- [x] 2.4 Связать действие "Применить" с валидацией и созданием plist.
- [x] 2.5 Показывать диалоги успеха и ошибки с полезным текстом, включая путь созданного plist при успехе.

## 3. Проверка

- [x] 3.1 Добавить или обновить тесты для валидации, содержимого plist-payload, отсутствия WorkingDirectory, вывода путей логов и защиты от перезаписи существующего файла.
- [x] 3.2 Вручную проверить, что пункт меню открывает одно окно с четырьмя полями и кнопками "Отмена" / "Применить".
- [x] 3.3 Вручную проверить, что поток меню создает plist, совпадающий с запрошенной формой LaunchAgent.
- [x] 3.4 Запустить тесты проекта или syntax-check и записать результат.
