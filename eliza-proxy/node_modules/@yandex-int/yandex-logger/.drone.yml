pipeline:
  tests:
    image: node:10
    pull: true
    environment:
      - NODE_ENV=development
    commands:
        - echo "Running job for Node v.10"
        - rm -rf node_modules
        - npm install --registry=https://npm.yandex-team.ru --silent
        - npm test
