import { Middleware } from '..';

declare namespace middleware {
    export interface SecureFieldOptions {
        fields?: string[];
        secureValue?: (value: string) => string;
    }
}

declare function middleware(config?: middleware.SecureFieldOptions): Middleware;

export = middleware;
