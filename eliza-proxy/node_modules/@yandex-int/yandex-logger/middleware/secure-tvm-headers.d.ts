import { Middleware } from '..';

declare namespace middleware {
    export interface SecureTvmHeadersOptions {
        field?: string;
    }
}

declare function middleware(config?: middleware.SecureTvmHeadersOptions): Middleware;

export = middleware;
