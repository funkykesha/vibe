import { Middleware } from '..';

/**
 * Contains secure-tvm-headers, secure-passport-cookie middlewares
 * and secure-field middleware for `req.headers.authorization` field
 */
declare function preset (): Middleware[];

export = preset;
