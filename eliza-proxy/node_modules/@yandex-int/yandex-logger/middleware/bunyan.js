'use strict';

/**
 * Делает формат записей совместимым с bunyan pipes
 *
 * @returns {Function}
 */
module.exports = () => {
    /**
     * @param {Object} record
     * @returns {undefined}
     */
    return record => {
        record.v = 0;
        record.time = new Date();
    };
};
