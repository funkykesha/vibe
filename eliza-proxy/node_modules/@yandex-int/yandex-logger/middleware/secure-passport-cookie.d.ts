import { Middleware } from '..';

declare namespace middleware {
    export interface SecurePassportCookieOptions {
        field?: string;
        cookies?: string[];
    }
}

declare function middleware(config?: middleware.SecurePassportCookieOptions): Middleware;

export = middleware;
