import { OptionsInit } from 'got';
import { ConstructorOptions } from 'raven';
import { WritableLike } from '..';

declare namespace stream {
    export interface SentryStreamOptions {
        clientOptions?: ConstructorOptions;
        dsn?: string;
        transportOptions?: OptionsInit;
    }
}

/**
 * Sentry stream
 */
declare function stream(config?: stream.SentryStreamOptions): WritableLike;

export = stream;
