import { WritableLike } from '..';

declare namespace stream {
    export interface DeployStreamOptions {
        stream?: Writable;
        excludeFields?: string[];
    }
}

declare function stream(config?: stream.DeployStreamOptions): WritableLike;

export = stream;
