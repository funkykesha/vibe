import { ResolversMap, WritableLike } from '..';

declare namespace stream {
    export interface LineStreamOptions {
        resolvers?: ResolversMap;
        stream?: WritableLike;
        template?: string;
    }
}

declare function stream(config?: stream.LineStreamOptions): WritableLike;

export = stream;
