import { WritableLike } from '..';

declare namespace stream {
    export interface JsonStreamOptions {
        stream?: WritableLike;
    }
}

declare function stream(config?: stream.JsonStreamOptions): WritableLike;

export = stream;
