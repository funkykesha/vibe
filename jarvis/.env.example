# Jarvis secrets. Copy to .env and fill in. Never commit .env.
#
# Iteration 1 needs no keys: voice input is handled by Handy (local STT app),
# and Codex runs from your own CLI auth. These are placeholders for the
# conversational/streaming voice backend planned in later iterations.

# GEMINI_API_KEY=
