# Active Context

## Current Session

**Task:** Прототипировать Prompt Engineer — инструмент для создания и улучшения AI-промптов

**Status:** Все спринты завершены. UI отполирован. Следующий этап — подключение реального API.

### Что сделано

- ✅ Sprint 1 — Скелет + дизайн-система
- ✅ Sprint 2 — Ветка A (Branch A) — 5 вопросов → генерация промпта
- ✅ Sprint 3 — Ветка B (Branch B) — B1 вставка + B2a XML редактор + B2b трансформация
- ✅ Sprint 3.5 — UX-исправления
  - Тема Dark/Light: CSS переменные `:root[data-theme]`, кнопка ☀️/🌙
  - Кнопка 🌍 → EN в ResultScreen
  - Чипы-подсказки для "Тон и стиль" (6 вариантов) и "Формат вывода" (6 вариантов)
- ✅ Sprint 4 — UI-полировка кнопок
  - GhostBtn: цвет текста `var(--text)` (всегда читаем), обводка `var(--muted)` (видима)
  - Hover: фон `var(--surface-2)` вместо только смены рамки
  - `🌍 → EN` и `🔄 Сначала` — убраны тёмные dim-цвета

### Текущее состояние

- Оба branch работают (A и B)
- Все UI переходы работают
- Тема Dark/Light читаема в обоих режимах
- GhostBtn читаем без наведения (текст светлый, обводка видима)
- API — **Mock** (setTimeout + захардкоженные шаблоны)

### Следующие шаги

- **Приоритет 1:** Подключить реальный API (Groq / Anthropic / Gemini)
  - Добавить поле ввода API-ключа (или переменную окружения)
  - Заменить `callModel()` на реальный fetch
- **Приоритет 2:** Полировка для mobile
- **Приоритет 3:** Сохранение истории (localStorage)
