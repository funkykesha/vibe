# Specifications

This change is an internal refactoring with no new or modified capabilities.

The `/api/chat` and `/api/models` endpoints maintain their existing API contracts:
- Request format: unchanged
- Response format: unchanged
- Behavior from client perspective: unchanged

No specification files are required because the external interface is not changing.
